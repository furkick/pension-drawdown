{
  "name": "pension-calc",
  "version": "1.0.0",
  "description": "Retirement Tax & Drawdown Calculator",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "vitest run && vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.7",
    "autoprefixer": "^10.5.0",
    "gh-pages": "^6.1.1",
    "tailwindcss": "^3.4.3",
    "vite": "^5.2.0",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "chart.js": "^4.4.2"
  }
}
